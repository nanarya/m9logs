services:
  app:
    image: jekyll/jekyll
    command: jekyll serve --force_polling --livereload --trace
    # command: jekyll build
    volumes:
      - .:/srv/jekyll
    ports:
      - 3000:4000
      - 35729:35729
    environment:
      - JEKYLL_ENV:development
      - LANG:ja_JP.UTF-8
